{
  "name": "Get Rows",
  "plugin_description": "Integrates with Google Sheets to read, search, and write rows from Kizen agentic workflows.",
  "action_description": "Fetches rows from a sheet, keyed by header name. Optionally filter to rows whose filter_column cell exactly matches filter_value.",
  "runtime": "python-3-13",
  "action_type": "google_sheets_get_rows",
  "script": "script.py",
  "inputs": [
    {
      "name": "spreadsheet_id",
      "label": "Spreadsheet ID",
      "hint": "The ID from the sheet's URL: .../spreadsheets/d/{spreadsheet_id}/edit",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "spreadsheet_id"
    },
    {
      "name": "sheet_name",
      "label": "Sheet Name",
      "hint": "The tab name at the bottom of the spreadsheet, e.g. Sheet1.",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "sheet_name"
    },
    {
      "name": "filter_column",
      "label": "Filter Column",
      "hint": "Header name of the column to filter on. Leave blank to return every row.",
      "data_type": "string",
      "required": false,
      "input_source": "variable",
      "script_alias": "filter_column"
    },
    {
      "name": "filter_value",
      "label": "Filter Value",
      "hint": "Only rows whose filter_column cell exactly matches this value are returned. Required if filter_column is set.",
      "data_type": "string",
      "required": false,
      "input_source": "variable",
      "script_alias": "filter_value"
    }
  ],
  "outputs": [
    {
      "name": "rows",
      "label": "Rows",
      "hint": "JSON array string of row objects, each keyed by header name.",
      "data_type": "string",
      "script_alias": "rows",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "row_count",
      "label": "Row Count",
      "data_type": "number",
      "script_alias": "row_count",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    }
  ]
}
