{
  "name": "Append Row",
  "plugin_description": "Integrates with Google Sheets to read, search, and write rows from Kizen agentic workflows.",
  "action_description": "Adds a new row to the end of a sheet, keyed by header name.",
  "runtime": "python-3-13",
  "action_type": "google_sheets_append_row",
  "script": "script.py",
  "inputs": [
    {
      "name": "spreadsheet_id",
      "label": "Spreadsheet ID",
      "hint": "The ID from the sheet's URL: .../spreadsheets/d/{spreadsheet_id}/edit",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "spreadsheet_id"
    },
    {
      "name": "sheet_name",
      "label": "Sheet Name",
      "hint": "The tab name at the bottom of the spreadsheet, e.g. Sheet1.",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "sheet_name"
    },
    {
      "name": "header_row",
      "label": "Header Row",
      "hint": "Which row contains the column headers (1-indexed). Leave blank to default to 1.",
      "data_type": "string",
      "required": false,
      "input_source": "variable",
      "script_alias": "header_row"
    },
    {
      "name": "header_column",
      "label": "Header Column",
      "hint": "Which column the headers start in (1-indexed). Columns to the left are left untouched by the append. Leave blank to default to 1.",
      "data_type": "string",
      "required": false,
      "input_source": "variable",
      "script_alias": "header_column"
    },
    {
      "name": "row_data",
      "label": "Row Data",
      "hint": "JSON object string keyed by header name, e.g. {\"Name\": \"Jane\", \"Email\": \"jane@example.com\"}. Headers not included default to blank; keys that aren't real headers raise an error.",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "row_data"
    }
  ],
  "outputs": [
    {
      "name": "row_number",
      "label": "Row Number",
      "hint": "The real 1-indexed sheet row the new data landed on.",
      "data_type": "number",
      "script_alias": "row_number",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "spreadsheet_id",
      "label": "Spreadsheet ID",
      "data_type": "string",
      "script_alias": "spreadsheet_id",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    }
  ]
}
